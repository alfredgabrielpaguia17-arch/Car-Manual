{
  "name": "aa-auto-manual",
  "version": "1.0.0",
  "private": true,
  "description": "A&A Auto Manual responsive website with Firebase authentication and Firestore.",
  "scripts": {
    "dev": "npx serve . --listen 5000",
    "start": "npx serve . --listen 5000"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
